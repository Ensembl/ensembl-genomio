# Usage

Import the library as usual:

```python
import ensembl.io.genomio
```

## CLI reference

The following commands are installed as entry points to ease integrating common tasks into your workflow:

<!-- entrypoints-table:start -->
<!-- entrypoints-table:end -->

All scripts support `--help` for usage details and `--version` to return the library's version.
